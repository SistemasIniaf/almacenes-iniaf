# Dependencias (cada paquete tiene su propio node_modules)
node_modules/

# Builds
dist/
build/

# Variables de entorno (commitear solo .env.example)
.env
.env.*.local

# Logs
*.log

# Sistema operativo
.DS_Store
Thumbs.db

# Archivos temporales de Office (se crean al abrir un .docx)
~$*

# Volcados del sistema anterior: pesan ~86 MB y la tabla `personal` guarda
# contrasenas en texto plano. Se analizan fuera del repo (ver
# docs/analisis-sistema-anterior.md).
*.sql.gz
semilla*.sql
